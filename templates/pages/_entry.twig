{% extends [
    '_custom/' ~ entry.slug,
    '_layout'
    ] %}

{% block header %}
    {% if entry.headerImage.first() != null %}
        {% set header_image_desktop = entry.headerImage.first().url %}
        
        <header class="image_header" style="background-image: url({{ header_image_desktop }});">
            {{ entry.headerText }}
        </header>
    {% endif %}
{% endblock %}

{% block common %}

    Common content for subpages is here

{% endblock %}

{% block specific %}
    {{ parent() }}
    test
{% endblock %}